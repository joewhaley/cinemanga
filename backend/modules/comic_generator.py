def generate_comic(script: str, style: str) -> list[dict]:
    panels = generate_panel_instructions(script, style)
    comic = generate_comic(panels)
    return comic

def generate_panel_instructions(script: str, style: str) -> list[dict]:
    panels = []
    
    # Process script and style to generate panel instructions
    
    return panels

def generate_comic(panels: list[dict]) -> list[dict]:
    comic = []
    
    # Process panels to generate comic
    
    return comic